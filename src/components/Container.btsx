
import { type OctaneNode } from "octane";
import ScrollArea from "./ui/ScrollArea.btsx";

props {children}: {children: OctaneNode}
main(className='h-full overflow-hidden min-w-0 border-t-[0.5px] border-border bg-background')
  ScrollArea(className='h-full') #{children}
