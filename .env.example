# CoinMarketCap API key (https://pro.coinmarketcap.com). Server-side only:
# - `bun run dev` reads it from .env for the dev proxy (rsbuild.config.ts)
# - `bun run cf:dev` reads it from .dev.vars
# - production: `bunx wrangler secret put CMC_API_KEY`
CMC_API_KEY=
